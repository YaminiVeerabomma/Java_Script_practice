let name = "suresh";
let age = 20;
console.log(`${name}'s age is ${age}`);
//after 1 year surash age ? age +1 not correct way
age = age + 1;
console.log(`${name}'s age is ${age}`);
//age=age+1 this statmenu we use two appernds so we use apperators
++age; //one apperend
console.log(`${name}'s age is ${age}`);
